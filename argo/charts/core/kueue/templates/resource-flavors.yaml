{{- range .Values.gpuResource.flavors }}
apiVersion: kueue.x-k8s.io/v1beta2
kind: ResourceFlavor
metadata:
  name: {{ .name }}
  labels:
    {{- include "kueue.labels" $ | nindent 4 }}
spec:
  nodeLabels:
    {{- toYaml .nodeLabels | nindent 4 }}
---
{{- end }}
