apiVersion: v1
kind: Service
metadata:
  name: {{ include "presigner.fullname" . }}
  labels:
    {{- include "presigner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "presigner.selectorLabels" . | nindent 4 }}
