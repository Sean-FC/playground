apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: spark
  namespace: argo
spec:
  project: data
  destination:
    server: https://kubernetes.default.svc
    namespace: spark
    annotations:
      argocd-diff-preview/watch-pattern: |
        argo/charts/data/spark/templates/.*,
        argo/charts/data/spark/values.*\.ya?ml,
        argo/charts/data/spark/Chart\.yaml,
        argo/charts/data/spark/Chart\.lock
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
  source:
    repoURL: git@github.com:Sean-FC/playground.git
    targetRevision: main
    path: argo/charts/data/spark
    helm:
      valueFiles:
        - values.dev.yaml
