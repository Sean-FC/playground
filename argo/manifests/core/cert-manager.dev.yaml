apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
  namespace: argo
  annotations:
    argocd-diff-preview/watch-pattern: |
      argo/charts/core/cert-manager/templates/.*,
      argo/charts/core/cert-manager/values.*\.ya?ml,
      argo/charts/core/cert-manager/Chart\.yaml,
      argo/charts/core/cert-manager/Chart\.lock
spec:
  project: core
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
  source:
    repoURL: git@github.com:Sean-FC/playground.git
    targetRevision: main
    path: argo/charts/core/cert-manager
    helm:
      valueFiles:
        - values.dev.yaml
