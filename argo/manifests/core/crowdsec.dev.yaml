apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: crowdsec
  namespace: argo
spec:
  project: core
  destination:
    server: https://kubernetes.default.svc
    namespace: crowdsec
    annotations:
      argocd-diff-preview/watch-pattern: |
        argo/charts/core/crowdsec/templates/.*,
        argo/charts/core/crowdsec/values.*\.ya?ml,
        argo/charts/core/crowdsec/Chart\.yaml,
        argo/charts/core/crowdsec/Chart\.lock
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  source:
    repoURL: git@github.com:Sean-FC/playground.git
    targetRevision: main
    path: argo/charts/core/crowdsec
    helm:
      valueFiles:
        - values.dev.yaml
